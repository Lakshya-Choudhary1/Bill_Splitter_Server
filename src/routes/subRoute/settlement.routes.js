import { Router } from "express";
const router = Router();


// create payment
router.post(
"/groups/:groupId"
);


// history

router.get(
"/groups/:groupId"
);


// single settlement

router.get(
"/:settlementId"
);

//get all settlements
router.get(
"/"
);

export default router;